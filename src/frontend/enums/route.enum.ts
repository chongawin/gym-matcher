export enum Route {
  HOME = '/',
  POKEMON = '/pokemon',
  CATEGORIES = '/categories',
  PRODUCTS = '/products',
}

export enum RouteSlug {
  HOME = 'home',
  POKEMON = 'pokemon',
  CATEGORIES = 'categories',
  PRODUCTS = 'products',
}
